from enum import Enum


class Mode(Enum):
    DEFAULT = 'Default mode'
    HILLMAN = 'Hillmans` mode\n' \
              'When one gladiator kill another gladiator,\n' \
              'the first takes strong of the second'
